# صيدها — قواعد المشروع

لعبة «صيد الكلمة»: فريقان يتناوبان على تخمين كلمة عربية سرية. اللعبة كلها في
`kuwait-quiz/`. (اسم المستودع `padel-rush` و`cycling_roguelite.html` بالجذر من
استخدام أقدم — ما لهم علاقة باللعبة.)

## الأساسيات

- **ما فيه نظام بناء ولا `package.json`** — ملفات ثابتة تُفتح مباشرة. لا تضيف
  أدوات بناء ولا مجمّعات.
- **تعليقات الكود بالعربي**، وتشرح **ليش** مو **شنو**. رسائل الـcommit
  **إنجليزي بحروف صغيرة** (‏`git log` يوضّح النمط).
- **الاستضافة GitHub Pages من فرع العمل، مو من `main`.** ‏`main` فيه `README.md`
  و`cycling_roguelite.html` بس — ولا ملف من اللعبة. فالدفع للفرع ينشر مباشرة،
  وما فيه خطوة دمج مطلوبة. (لا تفترض إن `main` هو المنشور وتروح تدمج عليه.)

## كيف يشتغل صاحب المشروع — وذكّره بهذا

يراجع من **الجوال عبر جلسة سحابية** (claude.ai)، مو من تيرمنال محلي.

- **أسرع طريقة توريه شغلك**: `node tools/build-standalone.js` وأرسل له
  `صيدها.html`. يفتحه على جواله مباشرة بلا سيرفر ولا إعداد — أفضل من اللقطات
  لأنه يلمس الشي بحجمه الحقيقي. سوّها بعد أي تغيير بصري.
- **اللقطات تكفي** للتحقق السريع، بس قراره البصري يحتاج جهازه.

### معلّق: الإعداد المحلي على اللابتوب (ويندوز)

**ذكّره فيه لما يجي شغل تصميم يحتاج تكراراً كثيراً** — هناك يشوف التعديل
بثواني بدل دقائق، وهذا أكبر فرق بالتصميم لأن العائق ذوقه مو سرعة التنفيذ.

الحالة: تطبيق كلود **مثبّت** · Git **لا** (وقف على رسالة «Replacing in-use
files» لأن التطبيق كان ماسك `bash`) · ولا نسخة محلية من المستودع، ولهذا زر
**Local** ما يشتغل — يطلب مجلداً وما فيه واحد.

أقصر طريق يتخطى Git (للعرض فقط، بلا رفع):
```
https://github.com/lomi222222/padel-rush/archive/refs/heads/claude/kuwait-trivia-game-mlkct9.zip
```
يستخرجه بـDocuments، ثم **New ← Local** ويؤشّر على المجلد.

## البنية

| الملف | الدور |
|---|---|
| `js/wordle-core.js` | المنطق الخالص: النقاط، المحاولات، كيس الكلمات، الفئات. بلا DOM |
| `js/wordle-view.js` | الرسم المشترك بين الوضعين: الشبكة، الكيبورد، النوافذ، الفئات |
| `js/wordle.js` | متحكّم الوضع المحلي (جهاز واحد) |
| `js/wordle-online.js` | متحكّم الأونلاين |
| `js/net.js` / `js/net-local.js` | طبقة النقل: فايربيس، أو localStorage للاختبار |
| `js/words.js` | بنك الكلمات بالفئات |
| `js/category-art.js` | رسومات بطاقات الفئات بشاشة الإعداد |

المنطق يروح `wordle-core.js`، والرسم المشترك `wordle-view.js`. لا تكرر منطق بين
`wordle.js` و`wordle-online.js`.

## قيود التخطيط — كل وحدة كلّفت جولة تصحيح

- **أزرار المساعدات الثلاثة لازم يقعدون بسطر واحد على ٣٧٥ بكسل.** أي حرفين
  زيادة بأي ليبل يلفّون السطر وياكلون ~‎٣٠ بكسل من ارتفاع الشبكة. **الهامش
  المتبقي ٦ بكسل** (الصف ‎٣٢٥ والحاوية ‎٣٣١) — يعني ولا حرف زيادة.
  الباقي من «اكشف حرف» ينعرض برقم صغير (`.uses-left`) مو بكلمة، وكلّف **صفر**
  من ارتفاع الشبكة بالقياس (`.wordle-info` ‎١٣٧ ومساحة الشبكة ‎٩٥٩٩٠ قبل وبعد).
- **‏`.wordle-info` فيه `overflow-y:auto` و`#wordle-play-screen` فيه
  `overflow:hidden`** ⇒ أي عنصر `position:absolute` جوّاهم **ينقص**. لهذا
  النوافذ المنبثقة تنلصق بـ`document.body` بموضع `fixed` — استخدم
  `View.createPopover(anchor, fill, opts)` ولا تخترع وحدة جديدة.
- **عند قياس مكسب بالمساحة**: طول الكلمة عشوائي فيغيّر حجم الخانة. قارن
  **ارتفاع `.wordle-info` ومساحة `.wordle-grid-scroll`**، مو حجم الخانة.
- الجوال أولاً: ‏**390×844** هو المقاس الأساسي. الوضع العرضي يُفحص بعده.

## الأونلاين

- **الهوست هو السيرفر.** هو وحده يمسك الكلمة السرية ويقيّم التخمينات ويحسب
  النقاط، وينشر «حالة عامة منقّحة» ما فيها الجواب. باقي الأجهزة ترسم من الحالة
  وترسل **نوايا** بس.
- **‏`rooms/<code>/live` منفصلة عن `state` عمداً**: مستمع `value` على عقدة أب
  ينزّل العقدة كاملة مع أي تغيير جواها. لولا الفصل، كل ضغطة حرف تنزّل ~‎١٫٥ كيلو
  على كل جهاز بدل ~‎٤٥ بايت. **لا تدمجهم.**
- **أي تأثير تلقائي (فتح نافذة، صوت، حركة) يُعلّق على زيادة عدّاد — مو على
  إعادة الرسم.** فايربيس يعيد الرسم مع كل تحديث حالة. هذي انكسرت مرتين: صوت
  الفوز (‏`hasCelebratedEnd`) ونافذة السجل (‏`lastHintCount`).
- الترقيم (`mySeq`) يبدأ من `Date.now()` عشان يظل تصاعدي لو اللاعب حدّث الصفحة.

### معلّق: البوق وقت كتابة الخصم — ذكّره فيها لما نشتغل على البوق

طلب صاحب المشروع «لا تنساها». اليوم البوق ينضغط بأي لحظة، حتى لو الخصم بنص
كتابة الصف، و**يمسح الصف** ويعطي الدور للسارق. والأخطر إن الكتابة تنبث حرف
حرف لكل الأجهزة (عبر `live`)، فالسارق يقدر **يتفرّج لين يكتبون أغلب الكلمة،
ويضرب بوق، وينسخ الحروف**.

ناقشنا حلّين، و**القرار له وما انحسم**:
- **قاعدته**: مقفول إذا كاتبين حرفين أو أكثر وباقي لهم حرفين أو أقل، أو الصف
  كامل وما انرسل. عيبها: بالكلمة الطويلة يظل البوق يشتغل بنص الصف، فينسخ
  السارق نصها.
- **اقتراحي**: مقفول من ثاني حرف لين ينرسل الصف. تحفّظه: بالكلمة الطويلة
  «عادي يمكن يصيدون الكلمة بعد جم حرف»، فالقفل الكامل ظالم للسارق.

- **حل ثالث اقترحته بعدها، أقوى برأيي**: الفريق الخصم يشوف **نقاط بدل الحروف**
  وقت الكتابة (●●●○○)، والزميل يشوف الحروف عادي. كذا ثغرة النسخ تنسد من
  أساسها، والبوق يظل شغال بأي وقت بدون قفل. حدّه: الإخفاء بالعرض بس (فايربيس
  يوصّل الحروف لكل الأجهزة)، فاللي يفتح أدوات المطوّر يقدر يشوفها. وما له داعي
  بالوضع المحلي لأن الكل على نفس الجهاز.

وأي قفل فيه ثغرة بالعكس: الفريق يكتب حرفين ويقعد، فيسدّ البوق على خصمه،
خصوصاً إذا اللعبة بدون وقت.

لما نرجع لها، التنفيذ بأربعة أماكن:
- دالة بـ`wordle-core.js` تقرّر «مقفول؟» من الصف والمسافات
- حارس الهوست بـ`action === "boq"` بـ`wordle-online.js` (**الهوست هو الحكم**،
  الزر لحاله ما يكفي)، وحارس بـ`canBoq()`
- حالة الزر تتحدّث مع كل ضغطة: مستمع `roomRef("live")` بالأونلاين، و`handleKey`
  بالمحلي

## رسومات الفئات (`js/category-art.js`)

صاحب المشروع اعتمدها وحدة وحدة. أي فئة جديدة **لازم** تجيها رسمة، وإلا
`tests/test_category_art.js` يطيح.

- **بلا ذوات أرواح**: لا وجوه ولا عيون ولا أشخاص ولا حيوانات، لأن صور ذوات
  الأرواح حرام عنده. «حيوان» أثر قدم، «أسماء» ظل بلا ملامح، «لاعبين» قميص.
  **الروبوت مسموح** (الآلة ما فيها روح)، ولهذا «أنميات» رأس روبوت.
- **لا تنسخ شخصيات أو شعارات محمية.** المستودع علني والموقع منشور، فالرسمة
  تاخذ علامات النوع بس. «أنميات» روبوت بقرن V مو غاندام ولا غرندايزر، وقبلها
  كانت دوّامة من رسمنا مو شعار «كونوها».
- **بلا أي نص داخل الرسمة** (لا `<text>` ولا `<title>`). اختبارات كثيرة تلقى
  الفئة بمقارنة نص البطاقة حرفياً. **ولو لازم كتابة** (مثل «مناطق» على لوحة
  المنطقة) تنرسم مسارات من خط Cairo بـ`tools/text-to-path.py`.
- **ألوان اللعبة بس** (كحلي وذهبي ورملي)، حتى لو الشي الحقيقي لونه غير. لوحة
  المنطقة زرقاء وبرتقالية بالشارع، وطلبها صاحب المشروع بألوان اللعبة.
- نوعان: **LAYERED** طبقات ألوانها من CSS (الكحلي ينقلب فاتح بالداكن)، و**RICH**
  أجسام واقعية ألوانها ثابتة بالوضعين. الأسلوب المعتمد «طبقات» بعمق وإضاءة
  (تدرّجات معدنية وظل ناعم).
- التعريفات (تدرّجات وظلال) بـsvg واحد `#ca-defs` **مو `display:none`**، لأن
  المتصفح ما يرسم تدرّجاً من svg مخفي. والتدرّجات `userSpaceOnUse`، لأن
  التدرّج النسبي يخفي أي خط مستقيم (عرض صندوقه صفر).

## أشياء لا تُلمس

- **‏`apiKey` بـ`js/firebase-config.js` علني عمداً وآمن** — معرّف مشروع مو كلمة
  سر، والحماية الفعلية من قواعد قاعدة البيانات. **مو تسريب، لا تحاول تخفيه.**
- **كل النقاط من مضاعفات ٢٥** — قيد من صاحب المشروع، «‏١٨٨ ما يصير». يعتمد على
  **ترتيب العمليات بـ`finalScoreForAttempt`: نسبة الفئة أولاً ثم الخصم الثابت.**
  الخام مضاعف ١٠٠، و×‎٠٫٧٥ عليه يعطي مضاعف ٢٥، وطرح مضاعفات ٢٥ يحافظ عليها. عكس
  الترتيب يطلّع كسوراً **بصمت** (‏٥٨١٫٢٥ بلوحة النقاط). يحرسه
  `tests/test_score_grid.js` بمسح ‎١٩٩٢ حالة.
- **كل الشِل من مخزن واحد — لا تخلي الصفحات «شبكة أولاً» والملفات «مخزن أولاً».**
  الخلط يعطي **HTML جديد فوق JS قديم**، وهذا مو إزعاج بل صفحة مكسورة: أول ما
  أضفنا قائمة «عدد البوق» طلعت عند صاحب المشروع فاضية تقول `No Options`، لأن
  العنصر وصل والكود اللي يعبّيه لا. اسم المخزن (`saydha-vN`) هو وحدة الذرّية.
  يحرسها `tests/test_update.js` بنشر عنصر HTML مع الـJS اللي يعبّيه معاً.
- **‏`sw.js`: عند تغيير أي ملف بقائمة `SHELL`، ارفع `VERSION`** (حالياً `v23`)
  وإلا اللاعبين يظلون عالقين على النسخة القديمة.
- **رفع `VERSION` لوحده ما يكفي** — الي يوصّل الترقية فعلاً هو `js/pwa.js`:
  ‏`sw.js` ما فيه `skipWaiting` بالتثبيت عمداً، والصفحة هي الي تأذن بالاستلام
  (‏`postMessage({type:"SKIP_WAITING"})`) ثم تعيد التحميل عند `controllerchange`.
  بدون هالدورة اللاعب لازم يفتح اللعبة **مرتين** عشان يشوف أي تعديل — وهذي
  انكسرت فعلاً: جهاز محدّث وجهاز لا بنفس الرابط. ‏`tests/test_update.js` ينشر
  تحديثاً حقيقياً وسط التشغيل ويثبت إنه يوصل بفتحة وحدة.
- الترقية **تتأجل لو اللاعب بنص جولة** (حالة الجولة كلها بالذاكرة) — تنطبق
  بالفتحة الجاية. لا تشيل هالحارس.

## الاختبار

```bash
bash kuwait-quiz/tests/run.sh            # كل الاختبارات (~٢ دقيقة، ٤ بالتوازي)
bash kuwait-quiz/tests/run.sh popovers   # اللي اسمها فيه "popovers" بس
```

يرفع سيرفراً على ‎8951 (أو يستخدم الشغّال)، يمر على كل `test_*.js`، ويطفّيه
بعدها. **شغّله قبل أي commit** — صار ~‎٢ دقيقة فما فيه عذر تتخطاه. (كان ١٤
دقيقة، وبهالمدة كنت أكتفي بمرشَّح صغير — ودُفع `7cb90a0` وفيه ٤ اختبارات طايحة
ما انكشفت إلا بعد الدفع.)

- **‏`KW_JOBS`** يحدد كم اختباراً بالتوازي (افتراضي ٤). العزل جاهز: كل اختبار
  يفتح متصفحه بملف تعريف منفصل، والسيرفر للقراءة بس.
- **‏`KW_JOBS=1`** يرجّعه تسلسلياً بمخرجات مباشرة. **أي طيحة بالتوازي تُعاد فيه
  قبل ما تُصدَّق**: نجحت تسلسلياً ⇒ تقلّب من التزاحم، نزّل `KW_JOBS`. طاحت
  تسلسلياً ⇒ علّة حقيقية.
- الطقم المتقلّب أسوأ من البطيء — تبطّل تثق فيه وتتجاهل الطيحات.

- الاختبارات Playwright خام (مو إطار)، ومخرجاتها عربية بعلامات ✅/‼️ وعدّاد
  `fail` ثم `process.exit(fail ? 1 : 0)`. استخدم `makeChecker()` من
  `tests/_browser.js` — ولا تكتب سكربتاً يطبع بلا ما يفشل، هذا «نجاح» كاذب.
- المتصفح ومسار Playwright كلهم بـ`tests/_browser.js` — لا تكتب مسارات بالنص.
- **اختبار الأونلاين بلا فايربيس**: `?net=local&pid=<id>` ⇒ `js/net-local.js`
  (‏localStorage + BroadcastChannel). كل لاعب `pid` مختلف بنفس المتصفح.
- امنع الشبكة بالاختبارات: `page.route("**://*.googleapis.com/**", r => r.abort())`
- تخطّي الشرح الأول: `localStorage.setItem("kw-tutorial-seen", "1")` بـ
  `addInitScript`.
- اختبر **المكسب المقصود بالقياس** لا بالشكل — الأرقام هي الي تثبت إن الشغلة
  اشتغلت.

## بنك الكلمات — حالة المراجعة

صاحب المشروع يراجع الفئات وحدة وحدة. **راجَعت ومعتمدة**: مسرحيات · دولة ·
مناطق الكويت · حيوان · كرتون قديم · رياضة · طعام · ألعاب · مهنة · مكان ·
أسماء. **ما راجعناها بعد**: تاريخ · سيارات · ألوان.

### فئة «عام» — ناقصة عمداً، نرجع لها

شيلنا منها `سلطان` (اسم شخص وسط أشياء، وفيه فئة «أسماء» مستقلة) و`مراة`
(إملاء غلط — الصح «مرآة»، والمكتوب يتقرأ «امرأة»). **والباقي متروك مؤقتاً
بعلم المستخدم** — لا تعتبرها مراجَعة، وعلّتان فيها معروفتان:

1. **~‎٣٦٪ منها صفات مو أشياء** (جميلة، رخيصة، حزينة…). والتلميح الوحيد
   باللعبة هو اسم الفئة، و«عام» ما يعطي شي — فالصفة بلا صورة ذهنية يبني عليها
   اللاعب، عكس الأشياء الملموسة.
2. **الأخطر — تذبذب المذكر/المؤنث**: ‏٢٦ صفة تنتهي بـ«ة» وست بصيغة المذكر
   (‏`عطشان` `غضبان` `فرحان` `تعبان` `كسلان` `مجتهد`). اللاعب يعرف الكلمة وما
   يدري يكتب `جائع` ولا `جائعة` ⇒ حظ بحت يضيّع محاولة كاملة.

## أرشيف المحادثات

```bash
node tools/archive-chat.js <مسار-مستودع-الأرشيف>
```

نصوص الجلسات تقعد بحاوية الجلسة السحابية بس وتنمسح معها. السكربت يستخرج **نص
الحوار فقط** (بلا استدعاءات أدوات ولا مخرجاتها ولا لقطات) بملف لكل جلسة، ويعمل
commit ويدفع لو انعطي مستودع git. بلا وسيط، يكتب بمجلد مؤقت ويطبع مكانه.

- **الأرشيف يروح لمستودع خاص منفصل، ولا يُرفع هني أبداً.** ‏`padel-rush`
  **علني**، وفرع العمل نفسه هو الي ينشره GitHub Pages ⇒ أي ملف بالجذر ينحمّل من
  الموقع. نشر المحادثة غلطاً ما ينرجع.
- المستودع: **`lomi222222/saydha-archive-`** — لاحظ **الشرطة بالآخر**، جزء من
  الاسم فعلاً. بدونها `add_repo` يرجّع «ما انلقى».
- الخطوات: `add_repo` بصلاحية push، استنسخه، ثم `node tools/archive-chat.js`
  وعطه مسار النسخة (يكتب ويعمل commit ويدفع).
- **تأكد إنه خاص قبل ما تكتب فيه** — اقرأ `private` من الـAPI، لا تفترض.
- ملف لكل جلسة ⇒ إعادة التشغيل تحدّث الجلسة الحالية بس، بلا تعارض مع القديم.
- **رسائل رفض الأدوات تُلتقط كرسائل مستخدم** (‏`toolDenialKind`): هناك يصحّح
  صاحب المشروع المسار، وهي أثمن ما بالمحادثة. لا تشيل هالفرع.

## نسخة احتياطية بملف واحد

```bash
node tools/build-standalone.js
```

يولّد `صيدها.html` — ملف واحد فيه كل شي (CSS و JS والصوت والشعار)، يشتغل
بالدبل-كلك بلا سيرفر ولا نت. الوضع المحلي كامل؛ الأونلاين يحتاج نت. الملف
المولّد **ما يُرفع للمستودع** (‏`.gitignore`) — السكربت هو الي يُحفظ.
